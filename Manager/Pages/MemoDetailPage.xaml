<Page
    x:Class="Manager.Pages.MemoDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Manager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <!-- Don't realize our content by default. See PageRoot_Loaded. -->
    <Grid>
        <StackPanel x:Name="RootPanel" >
            <TextBlock
                Margin="8,0"
                Style="{ThemeResource TitleTextBlockStyle}"
                HorizontalAlignment="Center"
                Text="{Binding MemoItem.Title, Mode=OneWay}" />

            <TextBlock
                Margin="12,8"
                HorizontalAlignment="Left"
                MaxWidth="560"
                Style="{ThemeResource BodyTextBlockStyle}"
                Text="{Binding MemoItem.Text, Mode=OneWay}"
                EntranceNavigationTransitionInfo.IsTargetElement="True" />

        </StackPanel>
        
    </Grid>
    
</Page>
