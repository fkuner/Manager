<Page
    x:Class="Manager.Pages.MemoDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Manager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <!-- Don't realize our content by default. See PageRoot_Loaded. -->
    <Grid>
        <StackPanel x:Name="RootPanel" >
            <TextBox
                Margin="8,0"
                BorderThickness="0,0,0,0"
                HorizontalAlignment="Left"
                Width="100"
                Text="{Binding MemoItem.Title, Mode=TwoWay}" />
            <Line Name="Line"
                  Stroke="Red"
                  X2="400"/>
            <!-- <Button Content="保存" Command="{Binding SendCommand}" /> -->
            <TextBox
                BorderThickness="10,10,10,10"
                AllowFocusWhenDisabled="True"
                TextChanged="TextBox_OnTextChanged"
                Text="{Binding MemoItem.Text, Mode=TwoWay}" />
        </StackPanel>
        
    </Grid>
    
</Page>
